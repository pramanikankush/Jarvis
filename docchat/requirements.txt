fastapi
python-multipart
uvicorn[standard]
fastembed          # local embeddings (bge-small, ONNX) — first run downloads ~100 MB
pypdf
python-docx
python-pptx       # PowerPoint (.pptx) slide + notes extraction

# Document parser extras: OCR for scanned/image-only PDFs and image files
# (RapidOCR models ship inside the wheel — nothing is downloaded at runtime)
rapidocr-onnxruntime
pypdfium2          # renders PDF pages to images for OCR
pillow
httpx
pyjwt              # Clerk session JWT verification (ragchat/auth.py)
cryptography       # JWKS key loading for PyJWT (ragchat/auth.py)
pandas             # spreadsheet analysis
openpyxl           # .xlsx support for pandas
matplotlib>=3.9     # charts (headless Agg backend; 3.9+ for boxplot tick_labels)
ddgs               # free keyless web search (DuckDuckGo)
